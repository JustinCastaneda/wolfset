# wear/ — the real WOLFSET watch app (Kotlin / Compose for Wear OS)

Not started. Built in Phase 7, after the phone app's vertical slice.
Watch frames live in Figma at `node-id=123-3945`.

Do not copy code from `spike-hr/wear/` — the spike is throwaway; the mechanism it proves
(Health Services → Data Layer → phone) is what carries forward.
