<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <!-- Wear OS 6 (Android 16) moved heart rate under Health Connect: Health Services
         refuses to start an exercise without this one (seen on the Pixel Watch 4,
         2026-09-02: "Missing permissions: [android.permission.health.READ_HEART_RATE]"). -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <!-- ExerciseClient (an active workout session) needs this on top of BODY_SENSORS. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="WOLFSET"
        android:icon="@android:drawable/ic_menu_compass"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Sideloaded next to the phone app during development; no Play pairing flow. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The heart-rate stream: a health foreground service holding the exercise session. -->
        <service
            android:name=".HrStreamService"
            android:exported="false"
            android:foregroundServiceType="health" />
    </application>
</manifest>
