# mobile/ — the real WOLFSET Expo app

Not started. Bootstrapped in Phase 2 after the Phase 0 spike passes its gate.

Named `mobile/`, not `app/`, because expo-router claims `app/` for routes inside the project.
Do not copy code from `spike-hr/` — the spike is throwaway; only its findings carry forward
(`docs/spike-findings.md`).
