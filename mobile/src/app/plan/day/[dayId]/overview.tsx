import { DayOverviewScreen } from '@/features/set-loop/DayOverviewScreen';

// Route file stays thin (conventions §1): the day's pre-start overview lives in set-loop.
export default DayOverviewScreen;
